
import random



class Dice:
    def roll(self):
        first = random.randint(1,6)
        second = random.randint(1,6)
        return first, second

# creat a class object without constructor

dice = Dice()
print (dice.roll())
